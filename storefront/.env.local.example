# Server-side base URL for Django (used by Route Handlers + Server Components).
API_URL=http://localhost:8000
# Public base URL of the storefront itself (metadata, absolute links).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Exposed to the browser ONLY if a client ever needs the API origin. Same value in dev.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Secret for POST /api/revalidate (on-demand cache invalidation). Generate a long
# random value for prod; Django will send it as X-Revalidate-Secret (Plan-22).
REVALIDATE_SECRET=dev-only-change-me
# PayPal SANDBOX client id for the inline Buttons SDK (Plan-14b). Public by design — it
# ships to the browser; the client SECRET stays in the backend and must never appear here.
# Same value as the backend's PAYPAL_CLIENT_ID. Without it the PayPal option reports a
# failed attempt instead of rendering buttons. Provisioned by the human during payment
# certification — leave empty in local scaffolding.
NEXT_PUBLIC_PAYPAL_CLIENT_ID=

# Shared secret for POST /api/revalidate (instant CMS cache flush; must match the
# backend's REVALIDATE_SECRET).
# REVALIDATE_SECRET=

# Google Maps browser key (Plan-32b slice 3: Places autocomplete + pin map, NG only).
# Referrer-locked; see docs/runbooks/google-apis-setup.md. Empty = the address form
# is a plain form (the assist is never a gate).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
